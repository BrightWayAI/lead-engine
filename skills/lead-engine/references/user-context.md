# User Context — Lead Engine Plugin

> ⚠️ This file hasn't been set up yet.
>
> Run **`/lead-setup`** to fill it in. The rest of the plugin reads this file to personalize every draft, score every signal, and decide which tools to use.
>
> Until this is set up, the `/lead-*` commands will refuse to run.
