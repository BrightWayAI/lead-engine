# Sent Log — DMs & Replies

> Append-only log of every touch sent and reply received. Drives follow-up timing in `/lead-pipeline`.
>
> Maintained by `/lead-log`.

---

(empty — log your first send with `/lead-log SIG-[id] sent 1` once you've sent it)
